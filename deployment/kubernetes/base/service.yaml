apiVersion: v1
kind: Service
metadata:
  name: context-router
  labels:
    app.kubernetes.io/name: context-router
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: context-router
